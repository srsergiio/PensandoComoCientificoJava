package com.pensando.capitulo08;

import java.math.BigInteger;

/**
 * Ejercicio 8.4: Trabajando con BigInteger.
 */
public class MatematicasGrandes {

    /**
     * Calcula x elevado a la n usando BigInteger.
     * * @param x La base (entero normal).
     * @param n El exponente (entero normal).
     * @return El resultado como BigInteger (puede ser gigantesco).
     */
    public BigInteger potencia(int x, int n) {
        // TODO:
        // 1. Si n es 0, retornar BigInteger.ONE.
        // 2. Crear un BigInteger base = BigInteger.valueOf(x).
        // 3. Iterar n veces multiplicando el resultado.
        //    O usar el método base.pow(n) si quieres ser directo,
        //    pero el libro sugiere hacerlo con loops o recursión para practicar.
        //    Ejemplo loop: resultado = resultado.multiply(base);
        return null;
    }
}