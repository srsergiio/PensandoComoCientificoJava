package com.pensando.capitulo05;

/**
 * Ejercicio 5.4: Métodos con resultados (double).
 * Objetivo: Calcular la distancia euclidiana entre dos puntos (x1,y1) y (x2,y2).
 * Fórmula: raiz( (x2-x1)^2 + (y2-y1)^2 )
 */
public class Ejercicio5_4_Distancia {

    /**
     * Método auxiliar que calcula la suma de los cuadrados de dos números.
     * Ejemplo: sumaCuadrados(3.0, 4.0) debe devolver 25.0 (9 + 16).
     */
    public double sumaCuadrados(double a, double b) {
        // TODO: Calcular a*a + b*b y devolverlo.
        return 0.0;
    }

    /**
     * Calcula la distancia entre dos puntos.
     * DEBE utilizar el método sumaCuadrados.
     */
    public double distancia(double x1, double y1, double x2, double y2) {
        // TODO:
        // 1. Calcular la diferencia en x (dx) y en y (dy).
        // 2. Usar sumaCuadrados(dx, dy) para obtener la suma.
        // 3. Devolver la raíz cuadrada del resultado (Math.sqrt(...)).
        return 0.0;
    }
}